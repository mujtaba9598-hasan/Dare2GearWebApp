# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# raw photo source folders (imported into /public/photos via scripts/import-photos.mjs)
/PUNJAB
/D2G (9 DESTINATIONS)
/SINDH
/KPK
/BALOCHISTAN
/OTHER CITIES
/AJK
/kund malir & ziarat
/New Destinations
/*.zip
.dev-server.log

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# private accounts/ownership record — never push to GitHub
/ACCOUNTS.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code local settings
.claude/

# Lock file omitted: Cloudflare runs `npm install` (npm ci rejects the
# cross-platform @emnapi optional deps when the lock is built on Windows).
/package-lock.json
